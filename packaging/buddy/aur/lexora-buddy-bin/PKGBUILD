# Maintainer: Lexora maintainers

pkgname=lexora-buddy-bin
pkgver=0.1.0
pkgrel=1
pkgdesc="Lexora Desktop with a local Codex runtime and desktop pet"
arch=('x86_64')
url="https://github.com/haohaoxue-site/Lexora"
license=('AGPL-3.0-only')
depends=(
  'alsa-lib'
  'at-spi2-core'
  'cairo'
  'dbus'
  'desktop-file-utils'
  'expat'
  'gcc-libs'
  'glib2'
  'glibc'
  'gtk-layer-shell'
  'gtk3'
  'hicolor-icon-theme'
  'libcups'
  'libnotify'
  'libsecret'
  'libx11'
  'libxcb'
  'libxcomposite'
  'libxdamage'
  'libxext'
  'libxfixes'
  'libxkbcommon'
  'libxrandr'
  'libxss'
  'libxtst'
  'mesa'
  'nspr'
  'nss'
  'pango'
  'systemd-libs'
  'util-linux-libs'
  'xdg-utils'
)
provides=('lexora-desktop')
conflicts=('lexora-desktop')
options=('!strip' '!debug')
install="${pkgname}.install"
_deb_name="Lexora-${pkgver}-linux-amd64.deb"
_deb_url="${LEXORA_BUDDY_DEB_URL:-${url}/releases/download/v${pkgver}/${_deb_name}}"
_deb_sha256="${LEXORA_BUDDY_DEB_SHA256:-f34da7f8c4af2780755b35979e2c98e45c6652bc0d59bf05f03acab4cf9414de}"
source_x86_64=("lexora-${pkgver}-amd64.deb::${_deb_url}")
sha256sums_x86_64=("${_deb_sha256}")

package() {
  local data_archive

  bsdtar --no-same-owner -xf "${srcdir}/lexora-${pkgver}-amd64.deb" -C "${srcdir}"
  data_archive="$(find "${srcdir}" -maxdepth 1 -name 'data.tar.*' -print -quit)"
  test -n "${data_archive}"
  bsdtar --no-same-owner -xf "${data_archive}" -C "${pkgdir}"
}
